import math 
side = float(input("Enter the length of a side of the equilateral triangle: "))
length = float(input("enter the length of the prism: "))

area (math.sqrt(3) / 4) * side ** 2
volume = area * length

print(f"volume of triangular prism: ( volume:.2f)")
