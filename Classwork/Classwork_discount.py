"""
START

INPUT PURCHASE

IF PURCHASE > 1000 OR PURCHASE < 10000
    DISCOUNT_PERCENT = 5
    
    IF PURCHASE > 10000 OR PURCHASE 50000
        DISCOUNT_PERCENT = 10
        
      IF PURCHASE > 50000
      DISCOUNT_PERCENT =20
      
      DISCOUNT = PURCHASE * DISCOUNT_PERCENT/100
      
      STOP
      
      
      
      """""
      
      
   purcase= int(input("enter purchase: "))
   
   if purchase > 1000 or purchase < 10000:
        discount = 10 * purchase 
        print (discount)
    if purchase > 10000 or purchase < 50000
        discount = 10 * purchase
        print (discount)
        
     if purchase > 50000:
        discount = 10 * purchase
        print (discount 
        
       print(discount))
